
<div id="users">

  <div class="container">

    <h3 text-align="in-line"><%= current_user.name  %></h3>

    <div class="content">

      <div class="card">

        <h4 text="top-left" >Medicamentos disponíveis para doação</h4>
        <% @meds.each_with_index do |med, index| %>
        <p text-align="left">
        <%= "- #{index + 1}) #{med.name}: #{med.description}, val: #{med.exp_date.strftime("%m/%d/%Y")}" %>
        </p>
            <% end %>

      </div>

      <div class=card>

        <h4 text="top-left">Medicamentos disponiveis</h4>
        <p text-align="left">
                "data: 09/08/2017, medicamento: viagra, doador: Victor"
        </p>

      </div>

    </div>

    <div class="content">

      <div class="card">

        <h4 text="top-left" >Medicamentos disponíveis para doação</h4>
        <% @meds.each_with_index do |med, index| %>
        <p text-align="left">
        <%= "- #{index + 1}) #{med.name}: #{med.description}, val: #{med.exp_date.strftime("%m/%d/%Y")}" %>
        </p>
            <% end %>

      </div>

      <div class=card>

        <h4 text="top-left">Medicamentos disponiveis</h4>
        <p text-align="left">
                "data: 09/08/2017, medicamento: viagra, doador: Victor"
        </p>

      </div>

    </div>

  </div>

</div>





