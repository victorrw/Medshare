<h1>MY MEDICINES TO GIVE</h1>

<% @meds.each_with_index do |med, index| %>
<% medicament = "#{index + 1}) #{med.name}: #{med.description}, val: #{med.exp_date.strftime("%m/%d/%Y")}" %>
  <p>
  <%= link_to medicament, med_path(med) %>
  </p>
<% end %>
